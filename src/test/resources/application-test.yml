spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orders_db
    username: dbuser
    password: password
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: create
